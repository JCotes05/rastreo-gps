# Configuración de Nginx — Tu instancia (Ubuntu)
# Desplegado automáticamente en /etc/nginx/sites-available/default por GitHub Actions.
#
# ⚠️ Reemplaza "tu-dominio.ddns.net" por tu dominio real de No-IP en
# las 4 líneas donde aparece, antes del primer despliegue.

server {
    listen 80 default_server;
    listen [::]:80 default_server;

    if ($host = tu-dominio.ddns.net) {
        return 301 https://$host$request_uri;
    }

    server_name tu-dominio.ddns.net;
    return 404;
}

server {
    root /var/www/rastreo;
    index index.html;

    server_name tu-dominio.ddns.net;

    location /datos {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /recorrido {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location / {
        try_files $uri $uri/ =404;
    }

    listen [::]:443 ssl ipv6only=on; # managed by Certbot
    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/tu-dominio.ddns.net/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/tu-dominio.ddns.net/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot
}
